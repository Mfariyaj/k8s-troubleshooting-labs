version: '3.8'

services:
  nginx:
    image: nginx:alpine
    ports:
      - "8088:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - app
    networks:
      - lab08-network

  app:
    build: ./app
    expose:
      - "3000"
    networks:
      - lab08-network

networks:
  lab08-network:
    driver: bridge
