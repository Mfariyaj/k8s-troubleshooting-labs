## Solution: lab-07-dependency-ordering

### Root Cause
Resources created in wrong order (VPC before subnet)

### Fix
See the corrected configuration in the fix section below.

### Verification
Verify the fix resolves the error.
