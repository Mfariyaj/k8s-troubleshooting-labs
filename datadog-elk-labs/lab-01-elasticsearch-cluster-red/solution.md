## Solution: lab-01-elasticsearch-cluster-red

### Root Cause
ES cluster red: shard allocation failed, disk watermark

### Fix
See the corrected configuration in the fix section below.

### Verification
Verify the fix resolves the error.
