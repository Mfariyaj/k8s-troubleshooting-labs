## Solution: lab-03-sampling-dropping-all

### Root Cause
Tail sampling dropping all traces: wrong policy config

### Fix
See the corrected configuration in the fix section below.

### Verification
Verify the fix resolves the error.
