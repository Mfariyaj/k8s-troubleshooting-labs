## Solution: lab-08-ecr-push-denied

### Root Cause
Docker push to ECR fails: auth token expired, repo policy wrong

### Fix
See the corrected configuration in the fix section below.

### Verification
Verify the fix resolves the error.
