# Lab 01: SSH Connection Failure - BROKEN Inventory
# ⚠️ This inventory has INTENTIONAL ERRORS - fix them!

[webservers]
web1 ansible_host=localhost ansible_port=2222 ansible_user=deployer ansible_ssh_private_key_file=./fake_key.pem
web2 ansible_host=localhost ansible_port=2222 ansible_user=deployer ansible_ssh_private_key_file=./fake_key.pem

[webservers:vars]
ansible_python_interpreter=/usr/bin/python3

# HINTS:
# - What port are the Docker containers actually running on? (check docker ps)
# - What permissions does SSH require for private keys?
# - Is there a correct SSH key you could use instead?
